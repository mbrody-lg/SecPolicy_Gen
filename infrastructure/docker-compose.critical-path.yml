# Test-only credentials for the CI critical-path runner.
services:
  context-agent:
    environment:
      TESTING: "true"
      FLASK_SECRET_KEY: test-only-secret-key
      POLICY_CALLBACK_TOKEN: test-only-policy-callback-token
  policy-agent:
    environment:
      TESTING: "true"
      FLASK_SECRET_KEY: test-only-secret-key
  validator-agent:
    environment:
      TESTING: "true"
      FLASK_SECRET_KEY: test-only-secret-key
